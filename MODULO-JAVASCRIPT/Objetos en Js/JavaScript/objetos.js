//Objetos

const nombreProducto = 'Monitor 20 pulgadas';

const precio = 300;

const dispinible = true;


/*Objeto */
const producto = {
    nombreProducto: 'Monitor 20 pulgadas',
    precio: 300,
    disponible: true,
}

console.log(producto);

console.log(producto.precio);

//otra forma de mostrar las caracteristicas del objeto
console.log(producto[precio]);